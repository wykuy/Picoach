def load_config():
    # Load your configuration settings from a JSON file or default values
    return {
        "brightness": 100,
        "boat_type": "single"
    }

def save_config(config):
    # Save the updated configuration settings
    pass  # You can implement saving to a file here
